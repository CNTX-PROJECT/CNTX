# CNTX Context Packet Epistemic Provenance and Freshness Profile Definition Schema Resource (ARCH-044)

## In ordinary language

ARCH-043 defines one exact external record for applying the Accepted Context
Packet Epistemic Provenance and Freshness Profile. Accepted ARCH-044 translates
only that closed record into one machine-evaluable JSON Schema and fixes 72
synthetic structural examples whose expectations preceded validation.

| Quick view | Meaning |
| --- | --- |
| **Status** | Accepted and integrated Profile Definition Schema Resource `1.0.0` |
| **Subject** | One ARCH-043 application record governed by the exact Profile Definition `1.0.0` |
| **Machine result** | Schema-local structural valid or invalid for one exact candidate schema and materialized instance |
| **Not established** | External packet/task equality, complete source association, source truth, Profile conformance, approval, Tool support, release, deployment, or authority |

### Reading route

- [Identity and resource model](#accepted-definition-schema-identity-and-version)
- [Closed root and structural assertions](#closed-fourteen-member-root)
- [Cases](#synthetic-cases-fixed-before-execution)
- [What the schema cannot prove](#standard-json-schema-limitations)
- [Lifecycle boundary](#accepted-lifecycle-and-authority-boundary)

This visitor layer is non-normative and adds no requirement beyond the complete
Accepted decision below.

## Status and authority

**Document Status:** Accepted.

This decision is governed by
[issue #149](https://github.com/CNTX-PROJECT/CNTX/issues/149). Attributable
EIGENAAR / Final Authority acceptance of the exact issue contract is recorded
in issue comment
[5279967413](https://github.com/CNTX-PROJECT/CNTX/issues/149#issuecomment-5279967413).
The accepted issue body contains exactly 37,649 characters, 37,651 UTF-8 bytes,
and SHA-256
`f3228e98ec5b00bd2e95969dcb8f84ec5f0fecd81b0265df84c758f75f838567`.

Attributable source-preserving correction addendum
[5280408320](https://github.com/CNTX-PROJECT/CNTX/issues/149#issuecomment-5280408320)
corrects only the baseline link-inventory pin to exactly `1489 Markdown / 27
HTML` and `1297 local / 219 external`. The issue title and body remain untouched;
the baseline commit/tree remains unchanged; and no source meaning,
architecture, schema design, assertion, case, path, identity, version, resource
graph, validation requirement, status, or lifecycle boundary changes.

Attributable second source-preserving correction addendum
[5280832992](https://github.com/CNTX-PROJECT/CNTX/issues/149#issuecomment-5280832992)
corrects only the evaluation-responsibility count from sixteen to seventeen.
Those seventeen responsibilities remain exactly `evaluationIdentifier`,
`outcome`, `dimension`, `subject`, `capabilityRefs`, `sourceAssociationRefs`,
`claimRefs`, `governingSourcePins`, `policyRefs`, `conditionRefs`, `evidenceRefs`,
`observationAndReferenceContext`, `diagnostics`, `limitationRefs`,
`adverseAndRestrictedInformationRefs`, `nonExecutionRefs`, and
`responsibleRoleRefs`. The correction changes no member, meaning, schema
assertion, case, path, identity, version, resource graph, validation
requirement, status, or lifecycle boundary.

The exact public baseline is commit
`eec00d698512533c3d40f985fe5d588cd03438f1`, tree
`87a2d5aaf01e3f7c45b4fb4e3d8aa40a21dc046a`, with 202 tracked paths. ARCH-043
and ADR-0043 are Accepted and integrated through PR #148; issue #147 is
closed/completed and its exact task branch is absent locally and publicly.

Attributable exact-head candidate acceptance is recorded in issue comment
[5285702199](https://github.com/CNTX-PROJECT/CNTX/issues/149#issuecomment-5285702199),
bound to candidate commit
`7420e5d179ab965bfda58780df4f41a08a0b62de` and tree
`56d1808cb95f3dd5a0b5d84f2a8e440891dff5e6`.

Issue-contract acceptance authorized only the bounded Proposed candidate
lifecycle. That preceding Proposed status allocated or activated nothing.
Exact-head candidate acceptance and the status-only promotion established
Accepted status only. Status promotion, branch or repository presence, schema
or JSON parsing, meta-schema validity, case evaluation, Draft state, review, or
mergeability did not by themselves integrate, allocate, or activate the
Definition Schema Identifier, Schema Version, canonical `$id`, or Schema
Resource.

Separately attributable integration-authority comment
[5286010813](https://github.com/CNTX-PROJECT/CNTX/issues/149#issuecomment-5286010813)
authorized PR #150 integration of the unchanged Accepted candidate. PR #150 is
merged at public `main` commit
`1d9e4667d68cce6e0289464c821bcd95e1d355ae` and tree
`3feeb1ce8ce2c0a7b45b88e42c9d668fc856d367`. Completion comment
[5286062635](https://github.com/CNTX-PROJECT/CNTX/issues/149#issuecomment-5286062635)
records issue #149 as closed/completed and its exact task branch as absent
locally and publicly. Integration preserves all original Proposed, correction-
addendum, failed-validation, replacement-validation, candidate, acceptance,
promotion, execution, evidence, integration, completion, and cleanup pins.

## Purpose and exact decision boundary

Accepted ARCH-039 defines one narrowing-only Profile Definition with exactly
two Profile Subjects. Accepted and integrated ARCH-043 defines one external
closed fourteen-member JSON-compatible record that applies that Profile to one
exact Context Packet revision under one exact approved Task Contract revision.

Accepted ARCH-044 defines exactly one standalone JSON Schema Draft 2020-12
resource for that exact record and exactly one separate operation-
based synthetic case manifest. It translates only schema-local structure that
is justified by ARCH-039 and ARCH-043. It does not repair, reinterpret, broaden,
weaken, default, override, or silently complete either source.

The resource evaluates one Profile application/declaration record. It is not a
Profile Definition, Profile instance, Context Packet, Task Contract, Module
declaration, Core Artifact, Governing Definition Declaration, Definition
Package, activation root, policy, evidence record, validation output, review,
decision, approval, release, or deployment record.

## Exact decision basis and precedence

The decision remains subordinate to and preserves:

1. ARCH-039 and ADR-0039 as the Accepted Profile Definition and exact two-
   subject semantic source;
2. ARCH-043 and ADR-0043 as the Accepted and integrated closed application
   representation target;
3. ARCH-040 and ADR-0040 as the Accepted Module declaration representation that
   is referenced but never embedded or redefined;
4. ARCH-042 and ADR-0042 as the separate integrated Module Definition Schema
   Resource and immutable `48/8/40` manifest;
5. ARCH-028 through ARCH-033 for Extension Module/Profile sovereignty,
   identities, dependencies, resources, schemas, conformance, Tool limits, and
   final-human authority;
6. ARCH-023 and ARCH-024 for offline-first Schema Resource resolution and
   separate schema-local outcomes;
7. the Accepted Context Packet and Task Contract Definitions,
   representations, Schema Resources, and exact `1.0.0` pins; and
8. every Accepted Core schema, manifest, rule, binding, Tool/Implementation,
   evidence, release, security/privacy, and authority boundary.

If a property, token, assertion, limit, or case cannot be justified by those
exact sources and issue #149, it is outside this decision.

## Accepted Definition Schema identity and version

| Dimension | Exact Accepted value |
| --- | --- |
| Schema family | `CNTX Profile Definition Schema Family` |
| Definition Schema Identifier | `https://github.com/CNTX-PROJECT/CNTX/schemas/profiles/context-packet-epistemic-provenance-freshness` |
| Initial Schema Version | `1.0.0` |
| Canonical version-qualified `$id` | `https://github.com/CNTX-PROJECT/CNTX/schemas/profiles/context-packet-epistemic-provenance-freshness/1.0.0` |
| Governing Profile Definition Identifier | `https://github.com/CNTX-PROJECT/CNTX/profile-definitions/context-packet-epistemic-provenance-freshness` |
| Governing Profile Definition Version | `1.0.0` |
| Schema language and dialect | JSON Schema Draft 2020-12 |
| Media type | `application/schema+json` |
| Canonical repository path | `schemas/profiles/context-packet-epistemic-provenance-freshness/1.0.0/schema.json` |
| Document Status | Accepted and integrated through PR #150 under integration-authority comment `5286010813` |

The Definition Schema Identifier is version independent. Schema Version is a
separate exact semantic-version coordinate. The version-qualified `$id`
identifies the standalone resource; its HTTPS shape grants no
retrieval, redirect, network access, registry use, trust, support,
publication, acceptance, activation, or authority.

Profile Definition Version `1.0.0` and Schema Version `1.0.0` are independent.
Equal values create no lockstep lifecycle. The preceding Proposed issue, path,
file, `$id`, candidate, review, and validation presence allocated or activated
nothing. Accepted status without governed integration likewise does not
integrate, allocate, or activate either schema coordinate.

## One standalone resource with zero external references

The Accepted decision contains one canonical standalone root JSON Schema
Resource:

- exact root `$schema` and Accepted canonical `$id`;
- one public-safe `$comment`, title, and bounded description;
- exactly 52 internal reusable definitions, all below root `$defs`;
- exactly 207 static fragment-only internal `$ref` values, reaching all 52
  definitions;
- exactly zero external `$ref` values;
- no nested `$id`, public `$anchor`, `$dynamicRef`, `$dynamicAnchor`, custom
  dialect, custom vocabulary, Format-Assertion requirement, Hyper-Schema,
  remote resource, or retrieval instruction;
- closed objects, finite strings, arrays, integers, and nesting; and
- no defaults, coercion, normalization, inference, fallback, hidden repair,
  aggregate result, executable action, or authority-bearing annotation.

Zero external references is deliberate. The application contains exact pins
and opaque references to a Context Packet, Task Contract, Definition sources,
policies, evidence, authority sources, and ARCH-040 Module declarations; it
does not embed their complete records. A root-schema reference would evaluate
the wrong instance shape. Referencing another resource's internal `$defs`
would improperly couple this Profile to non-public internal schema structure.
The one-node resource graph is finite and acyclic by construction and adds no
Definition dependency.

The exact separate candidate reference inventory is therefore
`207 total / 207 internal / 0 external`. It is separate from, and changes none
of, the existing `1142/1133/9` reference inventory.

## Exact finite structural limits

The candidate applies these explicit bounds:

| Responsibility | Exact limit |
| --- | --- |
| General non-blank string | `minLength: 1`, `maxLength: 2048`, with at least one non-whitespace character |
| Identifier, revision, token, role label, opaque reference | `minLength: 1`, `maxLength: 512`, with at least one non-whitespace character |
| General reference or statement collection | `maxItems: 64` |
| Required non-empty reference or statement collection | `minItems: 1`, `maxItems: 64` |
| Role collection | `minItems: 1`, `maxItems: 32` |
| `application.supersedes` | `maxItems: 32` |
| `sourceAssociations` | `minItems: 1`, `maxItems: 64` |
| `contextPacketSourceIndex` | integer `0` through `63` |
| `selectedCapabilities` | `minItems: 1`, `maxItems: 64` |
| `conditions` and `evaluations` | each `maxItems: 128` |
| Derivation inputs and transformations | each `maxItems: 32` |
| Source-category set | `minItems: 1`, `maxItems: 6` |

Every collection described as duplicate-free uses `uniqueItems: true`. That
assertion proves only equality of complete JSON values, not uniqueness
projected onto one property or compound semantic key.

These limits are an explicit Profile narrowing choice. A Context Packet with
more than 64 selected sources cannot receive a structurally valid ARCH-044
application record. It must fail without truncation, sampling, splitting,
fallback, or silent omission. Finite structure proves no runtime memory, CPU,
time, recursion, expansion, or denial-of-service safety.

## Closed fourteen-member root

Every evaluated instance is one closed object with all and only:

1. `governingProfileDefinition`;
2. `profileSubjects`;
3. `application`;
4. `targetContextPacket`;
5. `governingTaskContract`;
6. `sourceAssociations`;
7. `selectedCapabilities`;
8. `governingContext`;
9. `supply`;
10. `activationContext`;
11. `conditions`;
12. `evaluations`;
13. `limitations`; and
14. `authority`.

Every root member is required. All root and subordinate objects are closed.
Unknown members, namespace escape bags, vendor fields, JSON `null`, empty
strings, defaults, repair, and coercion are invalid where a required exact
value or explicit condition is required.

## Exact governing Profile and subject pins

`governingProfileDefinition` is closed to exactly:

- `definitionIdentifier`, fixed to the governing Profile Definition Identifier;
- `definitionVersion`, fixed to `1.0.0`;
- `definitionCategory`, fixed to `CNTX Profile Definition`; and
- one non-blank `authoritativeSourceRevision`.

`profileSubjects` is closed to exactly
`contextPacketContractDefinition` and
`epistemicProvenanceFreshnessModuleDefinition`. Each is closed to exact
`definitionIdentifier`, `definitionVersion`, and non-blank
`authoritativeSourceRevision` pins. The exact subject constants are:

| Subject | Identifier | Version |
| --- | --- | --- |
| Context Packet Contract Definition | `https://github.com/CNTX-PROJECT/CNTX/contract-definitions/context-packet` | `1.0.0` |
| Epistemic Provenance and Freshness Module Definition | `https://github.com/CNTX-PROJECT/CNTX/extension-module-definitions/epistemic-provenance-freshness` | `1.0.0` |

The pins cause no retrieval and prove no existence, authenticity, acceptance,
activation, compatibility, support, or authority.

## Application, target, and governing task

`application` is closed to exactly `applicationIdentifier`,
`applicationRevision`, `relationship`, `supersedes`, `scope`, and
`nonAggregation`:

- non-blank stable opaque application identity and exact revision;
- relationship token `external-profile-application`;
- at most 32 duplicate-free exact prior identity/revision pins;
- `scope` is closed to `purpose`, `intendedUse`, `materialityBoundary`, and
  one non-empty bounded `prohibitedUses` collection; and
- `nonAggregation` is closed to `technicalMembersRemainSeparate`,
  `evaluationOutcomesRemainSeparate`, and `aggregationProhibited`, each fixed
  to boolean `true`.

Application identity is not Profile Definition identity, Profile-instance
identity, Artifact Instance identity, schema identity, approval, or authority.

`targetContextPacket` is closed to `artifactInstanceIdentifier`,
`artifactRevision`, `contractDefinitionIdentifier`,
`contractDefinitionVersion`, `schemaIdentifier`, `schemaVersion`, and
`authoritativeSourceRevision`. Its contract and schema coordinates are fixed
to the Accepted Context Packet `1.0.0` values.

`governingTaskContract` is closed to `artifactInstanceIdentifier`,
`artifactRevision`, `contractDefinitionIdentifier`,
`contractDefinitionVersion`, `schemaIdentifier`, `schemaVersion`,
`authoritativeSourceRevision`, and one non-blank `approvalReference`. Its
contract and schema coordinates are fixed to the Accepted Task Contract
`1.0.0` values.

| Object | Fixed contract Definition Identifier/Version | Fixed Schema Identifier/Version |
| --- | --- | --- |
| `targetContextPacket` | `https://github.com/CNTX-PROJECT/CNTX/contract-definitions/context-packet` / `1.0.0` | `https://github.com/CNTX-PROJECT/CNTX/schemas/context-packet/1.0.0` / `1.0.0` |
| `governingTaskContract` | `https://github.com/CNTX-PROJECT/CNTX/contract-definitions/task-contract` / `1.0.0` | `https://github.com/CNTX-PROJECT/CNTX/schemas/task-contract/1.0.0` / `1.0.0` |

Those objects identify one bounded external context only. Schema validity does
not prove that either Artifact Instance exists, that either complete record is
valid, or that the task pin equals the pin inside the external Context Packet.

## Source associations

`sourceAssociations` is a non-empty array of at most 64 closed association
objects. Every object requires exactly these sixteen ARCH-043 members:

1. `associationIdentifier`;
2. `contextPacketSourceIndex`;
3. `sourceReference`;
4. `claimRoles`;
5. `sourceCategories`;
6. `materiality`;
7. `moduleDeclaration`;
8. `applicability`;
9. `provenance`;
10. `temporal`;
11. `policies`;
12. `integrity`;
13. `derivation`;
14. `conditionRefs`;
15. `limitationRefs`; and
16. `authorityRefs`.

Only these six source-category tokens are valid:

`governing-source`, `observation-source`, `evidence-source`,
`derived-source`, `human-assertion-source`, and
`model-recollection-source`.

`contextPacketSourceIndex` is limited to integer `0` through `63`. It is only a
locator inside one exact external packet revision and creates no semantic
order, precedence, priority, stable cross-revision identity, or authority. The
repeated `sourceReference` remains an exact string, not presumed unique.

`claimRoles` is a non-empty collection of closed `role` and `claimBoundary`
pairs. `sourceCategories` is a non-empty duplicate-free selection from the six
tokens above.

`materiality` is closed to `taskContractRelativeBoundary`,
`materialSourceBoundary`, `claimBoundary`, and `rationale`.
`moduleDeclaration` is closed to `declarationIdentifier`,
`declarationRevision`, and `authoritativeRepresentationSourceRevision`; it
does not embed, copy, redefine, or replace the thirteen-member Module
declaration. `applicability` is closed to `applicabilityContext`,
`profileSubjectRefs`, `narrowingStatement`, and `conditionRefs`;
`profileSubjectRefs` contains one or two duplicate-free references.

The closed `provenance` object keeps separate `originRefs`, `custodyRefs`,
`acquisitionRefs`, `observationRefs`, `supplyRefs`, `transformationRefs`,
`responsibleRoleRefs`, `limitationRefs`, `adverseInformationRefs`, and
`restrictedInformationMetadataRefs`. The closed `temporal` object keeps
`sourcePublicationRevisionTimeRefs`, `observationRetrievalTimeRefs`,
`recordProductionTimeRefs`, `validThroughTimeRefs`, and
`clockReferenceProvenanceRefs` separate. `policies` keeps separate
`freshnessPolicyPins` and `applicabilityPolicyPins`; each pin is closed to
`policyIdentifier`, `policyVersion`, `authoritativeSourceRevision`, and
`applicabilityContextRef`. Each member of `integrity.digestClaimRefs` is closed
to `digestClaimReference`, `algorithmIdentifier`, `subjectBoundary`,
`verificationContextRef`, `evidenceRefs`, and `limitationRefs`. `derivation`
keeps bounded `inputSourceRefs` and `transformationRefs` without defining or
performing a transformation.

## Selected capabilities

`selectedCapabilities` is a non-empty array of at most 64 closed narrowing
declarations. Each requires exactly `capabilityIdentifier`, `dimension`,
`subject`, `narrowingStatement`, `sourceAssociationRefs`, `claimRefs`,
`policyRefs`, `conditionRefs`, `evaluationRefs`, `limitationRefs`, and
`authorityRefs`. `sourceAssociationRefs` and `authorityRefs` are non-empty;
every collection remains finite and duplicate-free.

Only the seventeen Accepted dimensions are valid:

`source-category`, `source-identity`, `source-revision`,
`source-availability`, `provenance`, `authenticity`, `integrity`,
`source-publication-revision-time`, `observation-retrieval-time`,
`record-production-time`, `valid-through-time`, `freshness`, `applicability`,
`clock-reference-provenance`, `derivation`, `validation`, and `evidence`.

The schema can recognize these tokens and closed local structure. It cannot
prove that a natural-language narrowing adds no meaning or traces correctly to
one or both exact Profile Subjects.

## Governing context, supply, and activation

`governingContext` is closed to exact `contextIdentifier`, `contextRevision`,
`declarationSetReference`, `declarationSetRevision`, `definitionSourcePins`,
`prerequisiteRefs`, `conflictRefs`, and `authorityRefs`. It references but does
not serialize a generic Governing Definition Declaration Set, package,
registry, or activation syntax.

`supply` is closed to all eight ARCH-043 members: `supplierRoleRefs`,
`supplyProvenanceRefs`, `suppliedDefinitionSourcePins`,
`suppliedModuleDeclarationPins`, `targetContextPacketSupplyRef`,
`governingTaskContractSupplyRef`, `restrictionAndAccessConditionRefs`, and
`limitationRefs`. Required collections are finite and duplicate-free.

Restriction or limitation reference arrays may be empty only where a separate
explicit Assessed None condition records that assessment. The standalone
schema cannot prove resolution of that cross-record reference relationship.

`activationContext` is closed to `activationContextIdentifier`,
`activationContextRevision`, `applicationRelationship`, `prerequisiteRefs`,
`conflictRefs`, `nonExecutionRefs`, and `authorityRefs`.
`applicationRelationship` is exactly `referenced-prerequisite-context`. The
object is not an activation root and cannot activate itself, a Profile,
Module, Artifact, processor, or consequential action.

## Conditions and evaluations

`conditions` is a duplicate-free array of at most 128 separate closed
declarations. Each item requires exactly `conditionIdentifier`, `condition`,
`dimension`, `subject`,
`sourceAssociationRefs`, `claimRefs`, `evidenceRefs`, `limitationRefs`,
`responsibleRoleRefs`, `dependentCapabilityRefs`, and `explanation`.

Only these eight condition tokens are valid:

`specified`, `assessed-none`, `not-assessed`, `missing`, `inaccessible`,
`conflicting`, `restricted`, and `unverifiable`.

`evaluations` is a duplicate-free array of at most 128 separate closed
declarations. Each item requires exactly `evaluationIdentifier`, `outcome`,
`dimension`, `subject`,
`capabilityRefs`, `sourceAssociationRefs`, `claimRefs`,
`governingSourcePins`, `policyRefs`, `conditionRefs`, `evidenceRefs`,
`observationAndReferenceContext`, `diagnostics`, `limitationRefs`,
`adverseAndRestrictedInformationRefs`, `nonExecutionRefs`, and
`responsibleRoleRefs`.

Only these four outcomes are valid:

`satisfied`, `not-satisfied`, `unverifiable`, and `not-evaluated`.

Assessed None is not Missing. Not Assessed is not Assessed None. Inaccessible
is not absence. Restricted is not proof. An Unverifiable condition is not
automatically an Unverifiable evaluation, and Unverifiable is not Not
Satisfied.

No condition, outcome, count, order, diagnostic, schema result, or processor
output creates an aggregate pass/fail, score, percentage, threshold, traffic
light, grade, badge, quality gate, recommendation, approval, certification,
release fitness, deployment fitness, or consequential authority.

## Limitations and authority

`limitations` is closed to exactly these eleven required categories:

1. `scopeLimitations`;
2. `minimalityLimitations`;
3. `sourceCoverageLimitations`;
4. `methodLimitations`;
5. `evidenceLimitations`;
6. `accessLimitations`;
7. `securityPrivacyLimitations`;
8. `resourceLimitations`;
9. `adverseInformation`;
10. `restrictedInformationMetadata`; and
11. `unknownUnsupportedOrNonExecuted`.

Each category is either a non-empty bounded duplicate-free collection of
closed attributable limitation declarations or one explicit closed
`assessed-none` declaration. Restricted metadata is public-safe orientation
only and never substitutes for protected information.

Each attributable limitation declaration is closed to
`limitationIdentifier`, `subject`, `statement`, `sourceAssociationRefs`,
`capabilityRefs`, `conditionRefs`, `evaluationRefs`, `evidenceRefs`, and
non-empty `responsibleRoleRefs`. Each explicit Assessed None declaration is
closed to `condition: assessed-none`, `explanation`, `evidenceRefs`, and
non-empty `responsibleRoleRefs`.

`authority` is closed to non-empty bounded collections for exactly:

`declaringRoles`, `selectingRoles`, `supplyingRoles`, `applyingRoles`,
`evaluatingRoles`, `reviewingRoles`, `governingRoles`, and
`finalHumanAuthorityRoles`.

Every role reference is closed to opaque `identity`, `role`, bounded `scope`,
and `attributionRef`. It creates no person, account, credential, authentication,
authorization, signature, delegation, vote, approval, or identity-verification
system.

`automaticAuthority: false` remains a semantic invariant and is not a
serialized member. Because all affected objects are closed, a supplied
`automaticAuthority` property is invalid. Schema validity never becomes final-
human authority.

## Synthetic cases fixed before execution

The non-normative manifest contains one complete `baseInstance` and exactly 72
named operation-based cases with fixed boolean expectations:

- 11 expected valid; and
- 61 expected invalid.

The fixed manifest root contains exactly `schemaId`,
`definitionSchemaIdentifier`, `candidateSchemaVersion`, `governingDefinition`,
`documentStatus`, `nonNormative`, `issue`,
`issueContractAcceptanceComment`, `sourcePreservingCorrectionAddendumComment`,
`description`, `caseInventory`, `caseConstruction`, `baseInstance`, and
`cases`. The two governance coordinates are the numeric values `5279967413`
and `5280408320`, respectively. Recording the source-preserving addendum
coordinate changes no schema assertion, operation, case expectation, or
lifecycle boundary.
Second source-preserving count-correction addendum `5280832992` is governing
provenance recorded by this architecture source, not an additional manifest
field. It changes no manifest shape, operation, case, expected boolean, or
schema assertion.
`caseInventory` records exactly `72/11/61` and `aggregateAuthority: false`;
`caseConstruction` records only ordered `add`, `remove`, and `replace`
operations. The base instance contains exactly the same fourteen root members
as the schema. Every case contains exactly `name`, boolean `valid`, and
`operations`; all 72 names are unique. These manifest annotations and expected
booleans remain non-normative and non-authoritative.

Every case deep-copies the base instance and applies only ordered RFC 6901
`add`, `remove`, or `replace` operations. These operations are deterministic
test mechanics only. They are not a Profile representation, patch protocol,
migration, Serialization Binding, runtime, or implementation contract.

The eleven valid cases cover the minimal one-source form, empty condition and
evaluation sets with explicit non-execution, repeated source references at
distinct indices, all six source categories, all seventeen dimensions, all
eight conditions, all four outcomes, finite derivation, exact policy/digest
references, public-safe adverse/restricted metadata, and bounded supersession,
supply, activation, and authority context.

The 61 invalid cases cover root closure and requiredness, null and finite-bound
failures, exact Profile and subject pins, application scope and relationship,
target/task pins and approval, source-association structure, index type/range,
source-category closure and uniqueness, Module-reference-only structure,
capability dimensions and references, governing context, supply, activation,
conditions, evaluations, all limitation responsibilities, role collections,
and the prohibited serialized `automaticAuthority` property.

Each invalid case normally introduces one isolated schema-local failure. No
case pretends to test external packet/task equality, complete association,
projected-key uniqueness, reference resolution, graph semantics, source truth,
Profile conformance, or authority.

Historical Core cases remain separately `203/38/165`; ARCH-042 remains
separately `48/8/40`. The manifest-construction inventory changes only from
`10 direct / 1 operation-based` to `10 direct / 2 operation-based`. The new
`72/11/61` and descriptive combined `323/57/266` counts are not a score,
threshold, pass rate, completeness claim, conformance verdict, certification,
release gate, deployment gate, or authority.

## Evaluation and Tool boundary

The candidate schema, one complete base instance, 72 operation sets, and all
expected results were immutable before the separately bounded candidate
validation permitted by issue #149. The final authorized local validation used
one non-independent native-Windows environment, exact 64-bit CPython `3.13.14`,
and the five exact dependency wheels pinned by
`tools/minimal-validation-integrity-slice/requirements-1.0.1.lock`. It verified
interpreter provenance and each wheel's exact filename, byte size, and SHA-256
before one isolated offline hash-required installation, passed the Draft
2020-12 meta-schema check, materialized and evaluated all 72 cases exactly once,
and matched all `72/72` expectations (`11 valid / 61 invalid`) with zero
mismatches, `86` applied operations, and `207/207/0` total/internal/external
references. The complete raw transcript was read back in `222/222`
deterministic parts before acknowledgment-gated cleanup completed.

That validation remains local, non-governing, non-independent, and bound only
to accepted candidate commit/tree
`7420e5d179ab965bfda58780df4f41a08a0b62de` /
`56d1808cb95f3dd5a0b5d84f2a8e440891dff5e6`. This status-only promotion is not
a new execution or evidence instance. The private harness, environment, wheels,
logs, and outputs remained temporary and outside this repository. The existing
minimal validation and integrity Tool's exact ten-schema supported set, 29
unittest methods, thirteen rules, dependencies, scenarios, outputs, and
evidence remain unchanged and were not re-executed for ARCH-044.

The successful run is Windows-specific and non-independent. It does not
establish portability, interoperability, complete coverage,
performance, resource safety, production-data safety, conformance, support,
release fitness, or deployment fitness.

## Standard JSON Schema limitations

The resource can evaluate bounded local structure. It cannot prove:

- existence, availability, acceptance, authenticity, validity, or approval of
  any referenced Context Packet, Task Contract, Module declaration, Definition
  source, policy, evidence item, or authority source;
- equality between `governingTaskContract` and the pin inside the external
  target Context Packet;
- that every selected source in the external packet is associated exactly
  once, an index is in range for that packet, or the repeated source reference
  matches the value at that index;
- uniqueness projected onto association, capability, condition, evaluation,
  or compound keys when otherwise different objects are supplied;
- resolution, completeness, or consistency of opaque local references;
- that an empty supply restriction/limitation reference array is backed by an
  applicable separately declared Assessed None condition;
- external Definition, declaration, dependency, prerequisite, supersession,
  conflict, or derivation graph completeness or acyclicity;
- that a narrowing statement only narrows its exact two subjects;
- source truth, materiality, provenance, custody, acquisition, authenticity,
  integrity, digest correctness, freshness, applicability, clock correctness,
  policy applicability, derivation correctness, evidence sufficiency, privacy,
  safety, role identity, attribution, access, disclosure, or final authority;
- Core validity, Profile conformance, implementation support, interoperability,
  compatibility, certification, release fitness, or deployment fitness; or
- bounded runtime resources merely from finite instance structure.

No custom keyword, hidden semantic validator, network lookup, or private rule
is introduced to disguise these limits. Cross-record integrity and semantic
conformance remain later and separately governed.

## Core sovereignty and protected history

This decision changes no Common Artifact Envelope, Core Artifact schema,
Context Packet or Task Contract contract/payload/schema, Module declaration or
ARCH-042 schema/cases, existing Schema Resource, existing test manifest,
practice JSON, Tool/Implementation source, dependency, lock, rule, workflow,
CI, setting, tag, Release, evidence instance, or historical authority record.

Core validity remains prerequisite. This separate Profile resource cannot make
Core-invalid input valid, repair a missing packet/task relation, add a Core
property, activate a Profile, or expand the existing Tool supported set.

All 32 baseline JSON files, historical Core `203/38/165`, ARCH-042 `48/8/40`,
existing `1142/1133/9` total/internal/external schema references, seven issue-
#126 pins, 21 historical/corrective Git-blob pins, thirteen rules, 29 unittest
methods, eight practice scenarios, settings, disabled Actions, zero workflows,
ruleset `20518984`, tag, immutable prerelease, and H2.4 state remain protected.

## Security, privacy, and resource boundary

Every identifier, reference, role, source value, policy pin, limitation,
diagnostic, operation, and instance value is untrusted data. The schema and
cases contain no credential, secret, private key, personal data, production
configuration, private path/context, restricted content, provider-specific
assumption, or remote-resolution instruction.

Restricted-information metadata must contain only minimum authorized public-
safe orientation and must not reveal protected information through paths,
identifiers, query strings, error text, timing, roles, guessable hashes, or
derivation detail.

Finite schema bounds reduce unbounded structural supply. They prove no parser,
memory, CPU, time, handle, thread, process, operating-system isolation, network
isolation, or denial-of-service guarantee.

## Accepted lifecycle and authority boundary

Issue-contract acceptance comment `5279967413`, as supplemented only by the
source-preserving link-pin correction in comment `5280408320` and the
evaluation-count correction in comment `5280832992`, authorized preparation of
one exact candidate, static validation, one immutable candidate
commit, the one separately bounded post-commit execution, one Draft PR, one
transparent non-independent `COMMENTED` review, and a stop at exact-head
candidate acceptance. Attributable exact-head acceptance comment `5285702199`
accepts only candidate commit/tree
`7420e5d179ab965bfda58780df4f41a08a0b62de` /
`56d1808cb95f3dd5a0b5d84f2a8e440891dff5e6`.

The preceding Proposed status consumed no active Schema Version. Candidate
preparation, schema or JSON parsing, meta-schema validity, operation
materialization, fixed expected results, validation, review, Draft state,
repository presence, and mergeability allocated or activated nothing.
Exact-head acceptance and the status-only promotion established Accepted status
only; integration remained separately governed and was later authorized by
comment `5286010813`. PR #150 integrated the unchanged Accepted decision at
public `main` commit/tree
`1d9e4667d68cce6e0289464c821bcd95e1d355ae` /
`3feeb1ce8ce2c0a7b45b88e42c9d668fc856d367`. Completion comment `5286062635`
records issue #149 closed/completed and the task branch absent locally and
publicly.

A correction may fix documentary or schema error before exact-head acceptance
only within the one authorized candidate commit. Any later normative member,
token, assertion, limit, case, expected result, or structural change requires a
separately governed compatibility and version decision. Historical sources and
authority records remain source preserved.

Neither integration nor completion changed the accepted schema/case bytes,
repeated an execution, created evidence, or expanded the minimal Tool's exact
ten-schema supported set. All 12 Schema Resources and the separate Core
`203/38/165`, Module `48/8/40`, and Profile `72/11/61` inventories remain
unchanged. No Profile instance, Tool expansion, workflow, CI, release, tag,
publication outside the governed records, support, certification, hosting,
deployment, H2.4 completion, Phase 4A3.5, pilot, adapter, portability work,
adversarial evaluation, or later-phase authority follows.

## References

- [Context Packet Epistemic Provenance and Freshness Profile Definition](context-packet-epistemic-provenance-freshness-profile-definition.md)
- [Context Packet Epistemic Provenance and Freshness Profile JSON Representation Boundary](context-packet-epistemic-provenance-freshness-profile-json-representation-boundary.md)
- [Epistemic Provenance and Freshness Extension Module JSON Representation Boundary](epistemic-provenance-freshness-extension-module-json-representation-boundary.md)
- [Epistemic Provenance and Freshness Extension Module Definition Schema Resource](epistemic-provenance-freshness-extension-module-definition-schema-resource.md)
- [Extension Module and Profile Schema Resource, Packaging and Declaration Model](extension-module-profile-schema-resource-packaging-declaration-model.md)
- [Extension Module and Profile Executable Schema and Validation/Conformance Boundary](extension-module-profile-executable-schema-validation-conformance-boundary.md)
- [Schema Resource Resolution and Catalog Boundary](schema-resource-resolution-catalog-boundary.md)
- [Validation and Validation Output Contract](validation-and-validation-output-contract.md)
- [ADR-0044](adr/0044-context-packet-epistemic-provenance-freshness-profile-definition-schema-resource.md)
- [Accepted Schema Resource](../../schemas/profiles/context-packet-epistemic-provenance-freshness/1.0.0/schema.json)
- [Synthetic cases](../../tests/schemas/profiles/context-packet-epistemic-provenance-freshness/1.0.0/cases.json)
- [Governance](../../GOVERNANCE.md)
- [Security policy](../../SECURITY.md)

[task]
goal = "Describe the one concrete task"

[context]
include = ["README.md"]
required = ["README.md"]
exclude = [".git/**", ".opencntx/**", ".env*", "**/*.key", "**/*.pem"]
max_files = 10
max_bytes = 50000
